pipeline {
agent any
  stages {
    stage('SSH') {
      steps {
        sshPublisher(
          publishers: [
            sshPublisherDesc(
              configName: 'remote',
              transfers: [
                sshTransfer(
                  execCommand: """
                  mkdir /root/Mohamed
                  mkdir /root/Mohamed/Ahmed'
                  """
                )
              ]
            )
          ]
        )
      }
    }
  }
    post {
        always {
            cleanWs()
        }
        success {
            echo 'Build successful!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
