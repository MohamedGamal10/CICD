pipeline {
agent any
  stages {
    stage('SSH') {
      steps {
        sshPublisher(
          publishers: [
            sshPublisherDesc(
              configName: 'remote',
              transfers: [
                sshTransfer(execCommand: 'ls -al /root', printPty: true),
                sshTransfer(execCommand: 'pwd', printPty: true)
              ]
            )
          ]
        )
      }
    }
  }
    post {
        always {
            cleanWs()
        }
        success {
            echo 'Build successful!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
