pipeline {
agent any
  stages {
    stage('SSH') {
      steps {
        sshPublisher(
          publishers: [
            sshPublisherDesc(
              configName: 'remote',
              transfers: [
                sshTransfer(
                  execCommand: 'ls -al /root',
                )
              ]
            )
          ]
        )
      }
    }
  }
    post {
        always {
            cleanWs()
        }
        success {
            echo 'Build successful!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}
